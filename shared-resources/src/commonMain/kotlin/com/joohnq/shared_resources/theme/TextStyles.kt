package com.joohnq.shared_resources.theme

import androidx.compose.runtime.Composable
import androidx.compose.ui.text.SpanStyle
import androidx.compose.ui.text.TextStyle
import androidx.compose.ui.unit.sp

object TextStyles {
    @Composable
    fun headingLgExtraBold(): TextStyle =
        TextStyle(
            fontFamily = FontFamily.Urbanist.extraBold(),
            fontSize = 48.sp
        )

    @Composable
    fun textXlMedium(): TextStyle =
        TextStyle(
            fontFamily = FontFamily.Urbanist.medium(),
            fontSize = 20.sp
        )

    @Composable
    fun headingSpanSmExtraBold(): SpanStyle =
        SpanStyle(
            fontFamily = FontFamily.Urbanist.extraBold(),
            fontSize = 30.sp
        )

    @Composable
    fun heading2xlExtraBold() =
        TextStyle(
            fontFamily = FontFamily.Urbanist.extraBold(),
            fontSize = 72.sp
        )

    @Composable
    fun headingMdExtraBold() =
        TextStyle(
            fontFamily = FontFamily.Urbanist.extraBold(),
            fontSize = 36.sp
        )

    @Composable
    fun headingSmExtraBold() =
        TextStyle(
            fontFamily = FontFamily.Urbanist.extraBold(),
            fontSize = 30.sp
        )

    @Composable
    fun headingXsBold() =
        TextStyle(
            fontFamily = FontFamily.Urbanist.bold(),
            fontSize = 24.sp
        )

    @Composable
    fun headingXsSemiBold() =
        TextStyle(
            fontFamily = FontFamily.Urbanist.semiBold(),
            fontSize = 24.sp
        )

    @Composable
    fun headingXlExtraBold() =
        TextStyle(
            fontFamily = FontFamily.Urbanist.extraBold(),
            fontSize = 60.sp
        )

    @Composable
    fun headingXlBold() =
        TextStyle(
            fontFamily = FontFamily.Urbanist.bold(),
            fontSize = 60.sp
        )

    @Composable
    fun paragraphLg() =
        TextStyle(
            fontFamily = FontFamily.Urbanist.medium(),
            fontSize = 18.sp
        )

    @Composable
    fun paragraphMd() =
        TextStyle(
            fontFamily = FontFamily.Urbanist.medium(),
            fontSize = 16.sp
        )

    @Composable
    fun paragraphXs() =
        TextStyle(
            fontFamily = FontFamily.Urbanist.medium(),
            fontSize = 12.sp
        )

    @Composable
    fun paragraphSm() =
        TextStyle(
            fontFamily = FontFamily.Urbanist.regular(),
            fontSize = 16.sp
        )

    @Composable
    fun labelSm() =
        TextStyle(
            fontFamily = FontFamily.Urbanist.extraBold(),
            fontSize = 12.sp
        )

    @Composable
    fun displayLgExtraBold() =
        TextStyle(
            fontFamily = FontFamily.Urbanist.extraBold(),
            fontSize = 180.sp
        )

    @Composable
    fun displayMdExtraBold() =
        TextStyle(
            fontFamily = FontFamily.Urbanist.extraBold(),
            fontSize = 128.sp
        )

    @Composable
    fun displaySmExtraBold() =
        TextStyle(
            fontFamily = FontFamily.Urbanist.extraBold(),
            fontSize = 96.sp
        )

    @Composable
    fun text2xlExtraBold() =
        TextStyle(
            fontFamily = FontFamily.Urbanist.extraBold(),
            fontSize = 24.sp
        )

    @Composable
    fun text2xlBold() =
        TextStyle(
            fontFamily = FontFamily.Urbanist.bold(),
            fontSize = 24.sp
        )

    @Composable
    fun paragraph2Xl() =
        TextStyle(
            fontFamily = FontFamily.Urbanist.regular(),
            fontSize = 24.sp
        )

    @Composable
    fun text2xlSemiBold() =
        TextStyle(
            fontFamily = FontFamily.Urbanist.semiBold(),
            fontSize = 24.sp
        )

    @Composable
    fun textXlBold() =
        TextStyle(
            fontFamily = FontFamily.Urbanist.bold(),
            fontSize = 20.sp
        )

    @Composable
    fun textXlExtraBold() =
        TextStyle(
            fontFamily = FontFamily.Urbanist.extraBold(),
            fontSize = 20.sp
        )

    @Composable
    fun textXlSemiBold() =
        TextStyle(
            fontFamily = FontFamily.Urbanist.semiBold(),
            fontSize = 20.sp
        )

    @Composable
    fun textLgExtraBold() =
        TextStyle(
            fontFamily = FontFamily.Urbanist.extraBold(),
            fontSize = 18.sp
        )

    @Composable
    fun textLgBold() =
        TextStyle(
            fontFamily = FontFamily.Urbanist.bold(),
            fontSize = 18.sp
        )

    @Composable
    fun textLgMedium() =
        TextStyle(
            fontFamily = FontFamily.Urbanist.medium(),
            fontSize = 18.sp
        )

    @Composable
    fun textLgSemiBold() =
        TextStyle(
            fontFamily = FontFamily.Urbanist.semiBold(),
            fontSize = 18.sp
        )

    @Composable
    fun textMdExtraBold() =
        TextStyle(
            fontFamily = FontFamily.Urbanist.extraBold(),
            fontSize = 18.sp
        )

    @Composable
    fun textMdBold() =
        TextStyle(
            fontFamily = FontFamily.Urbanist.bold(),
            fontSize = 16.sp
        )

    @Composable
    fun textMdSemiBold() =
        TextStyle(
            fontFamily = FontFamily.Urbanist.semiBold(),
            fontSize = 16.sp
        )

    @Composable
    fun textMdMedium() =
        TextStyle(
            fontFamily = FontFamily.Urbanist.medium(),
            fontSize = 16.sp
        )

    @Composable
    fun textMdRegular(): TextStyle =
        TextStyle(
            fontFamily = FontFamily.Urbanist.regular(),
            fontSize = 16.sp
        )

    @Composable
    fun textSmExtraBold() =
        TextStyle(
            fontFamily = FontFamily.Urbanist.extraBold(),
            fontSize = 14.sp
        )

    @Composable
    fun textSmBold() =
        TextStyle(
            fontFamily = FontFamily.Urbanist.bold(),
            fontSize = 14.sp
        )

    @Composable
    fun textSmSemiBold() =
        TextStyle(
            fontFamily = FontFamily.Urbanist.semiBold(),
            fontSize = 14.sp
        )

    @Composable
    fun textSmMedium() =
        TextStyle(
            fontFamily = FontFamily.Urbanist.medium(),
            fontSize = 14.sp
        )

    @Composable
    fun textSmRegular() =
        TextStyle(
            fontFamily = FontFamily.Urbanist.regular(),
            fontSize = 14.sp
        )

    @Composable
    fun textXsExtraBold() =
        TextStyle(
            fontFamily = FontFamily.Urbanist.extraBold(),
            fontSize = 12.sp
        )

    @Composable
    fun textXsBold() =
        TextStyle(
            fontFamily = FontFamily.Urbanist.bold(),
            fontSize = 12.sp
        )

    @Composable
    fun textXsMedium() =
        TextStyle(
            fontFamily = FontFamily.Urbanist.medium(),
            fontSize = 12.sp
        )

    @Composable
    fun textXsRegular() =
        TextStyle(
            fontFamily = FontFamily.Urbanist.regular(),
            fontSize = 12.sp
        )

    @Composable
    fun text2XsRegular() =
        TextStyle(
            fontFamily = FontFamily.Urbanist.regular(),
            fontSize = 10.sp
        )
}
